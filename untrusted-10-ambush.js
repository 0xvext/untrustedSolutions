#Redefine the actions of the attackDone in the first code section:
	if (me.canMove('up')) {
    	me.move('up');
    }
    else if (me.canMove('left')) {
        me.move('left');
    }

#Redefine the actions of the reinforcementDrone to do nothing.

#Redefine the actions of the defenseDrone in the third code section:
 	if (me.canMove('up')) {
    	me.move('up');
    }
    else if (me.canMove('right')) {
    	me.move('right');
    }
